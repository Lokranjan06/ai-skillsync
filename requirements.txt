# ============================================================
# AI SkillSync — Production Requirements
# ============================================================

# --- Web framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9       # multipart file uploads

# --- Configuration ---
pydantic>=2.7.0
pydantic-settings>=2.2.0      # BaseSettings + .env loading
python-dotenv>=1.0.0          # .env file support

# --- Database ---
sqlalchemy>=2.0.0
alembic>=1.13.0               # database migrations

# --- AI / ML ---
scikit-learn>=1.4.0
numpy>=1.26.0

# --- Graph ---
networkx>=3.2.0

# --- Resume parsing ---
pypdf>=4.0.0

# --- HTTP client (testing / internal calls) ---
httpx>=0.27.0

# --- Dev / testing (excluded from prod Docker image via --no-dev) ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
